function setup() {
    createCanvas(1000,1000);
}

function draw(){
    background(0);
    fill(255);
    textSize(200);
    text("P",300,300); 
    if (mouseIsPressed) {
    fill(255);
    textSize(200);
    fill(255);
    textSize(13);
    text("lease help me this is reminding me too much of highschool math",340,300);  
    } else {
   
      }
  
}

