
function setup() {
    createCanvas(1000,1000);
}

function draw() {
    background(0);

    for (var x = 0; x < width; x = x + 10) {
        fill(70,180,100);
        textSize(10);
        text("p", x, 400);
    }

    for (var x = 0; x < height; x = x + 10) {
        fill(80,10,100);
        textSize(10);
        text("p", 400, x);
    }

    for (var x = 0; x < height; x = x + 10) {
        fill(100,10,230);
        textSize(10);
        text("p", 800, x);
    }

    for (var x = 0; x < height; x = x + 10) {
        fill(176,10,127);
        textSize(10);
        text("p", 130, x);
    }

    for (var x = 0; x < height; x = x + 10) {
        fill(20,20,200);
        textSize(10);
        text("p", 900, x);
    }
    for (var x = 0; x < height; x = x + 10) {
        fill(28,183,29);
        textSize(10);
        text("p", 171, x);
    }

    for (var x = 0; x < height; x = x + 10) {
        fill(80,10,100);
        textSize(10);
        text("p", 839, x);
    }

    for (var x = 0; x < height; x = x + 10) {
        fill(100,10,230);
        textSize(10);
        text("p", 247, x);
    }

    for (var x = 0; x < height; x = x + 10) {
        fill(176,10,127);
        textSize(10);
        text("p", 354, x);
    }

    for (var x = 0; x < height; x = x + 10) {
        fill(20,20,200);
        textSize(10);
        text("p", 237, x);
    }
    for (var x = 0; x < height; x = x + 10) {
        fill(28,183,29);
        textSize(10);
        text("p", 456, x);
    }

    for (var x = 0; x < width; x = x + 10) {
        fill(252,94,74);
        textSize(10);
        text("p", x, 835);
    }

    for (var x = 0; x < width; x = x + 10) {
        fill(81,32,73);
        textSize(10);
        text("p", x, 512);
    }

    for (var x = 0; x < width; x = x + 10) {
        fill(189,127,32);
        textSize(10);
        text("p", x, 489);
    }

    for (var x = 0; x < width; x = x + 10) {
        fill(76, 48, 75);
        textSize(10);
        text("p", x, 958);
    }

    for (var x = 0; x < width; x = x + 10) {
        fill(0, 76, 43);
        textSize(10);
        text("p", x, 37);
    }

    for (var x = 0; x < width; x = x + 10) {
        fill(31,78,73);
        textSize(10);
        text("p", x, 839);
    }

  for (var x = 0; x < 489; x = x + 100) {
    fill(255);
    textSize(1000);
    text('p', x, 500)
  }

}

